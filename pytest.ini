[pytest]
filterwarnings =
    # Suppress apispec warnings about multiple schemas with the same name.
    # This happens when using Marshmallow schemas with SQLAlchemy models
    # where multiple variations of the same model schema are registered.
    ignore:Multiple schemas resolved to the name.*:UserWarning:apispec.ext.marshmallow.openapi
